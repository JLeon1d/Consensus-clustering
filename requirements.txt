# Core dependencies for consensus-clustering
numpy>=1.24.0
scipy>=1.10.0
scikit-learn>=1.3.0
pandas>=2.0.0
pyyaml>=6.0

# Optional: Ray for parallelization
# Uncomment if you want Ray support
# ray>=2.9.0

# Development dependencies (for testing)
# Install with: pip install -r requirements.txt -r requirements-dev.txt